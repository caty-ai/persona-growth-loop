#!/usr/bin/env python3
# Drift-mirror baseline CLI; the growth baseline shim lives in bin/pgl-baseline.
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from mirror.baseline import main
raise SystemExit(main())
